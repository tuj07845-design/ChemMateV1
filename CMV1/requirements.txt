# ============================================================
# 注意：matlabengine 需与本机 MATLAB 版本严格匹配，
# 不适合放进通用依赖清单。请在部署机上单独安装：
#   pip index versions matlabengine     # 查看可用版本
#   pip install matlabengine==25.2      # R2025b 对应 25.x
# 若 PyPI 无匹配版本，从 MATLAB 安装目录装：
#   cd "C:/Program Files/MATLAB/R2025b/extern/engines/python"
#   python setup.py install
# ============================================================
openai
python-dotenv
flask
matplotlib
pillow
python-docx
python-pptx
pywin32
tavily-python